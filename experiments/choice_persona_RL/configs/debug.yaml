folder: "configs"
common_root: "base.yaml"
common_patch:
  name: "debug_run"
  wandb_project: "debug"
  function_kwargs:
    dataset_config:
      n_eval: 10
    training_config:
      logging_steps: 10
      num_generations: 8
      gradient_accumulation_steps: 1
      per_device_train_batch_size: 8
      eval_steps: 25
      max_steps: 100
      eval_on_start: true
      learning_rate: 1.0e-4
    saving:
      hf: false

experiments:
  - config: {}
